# After we have transform the df to this:

            # st.session_state["df_transform"] = df_transformed

# we want to apply expoential smoothing, arima, sarima on different tabs each on df_transformed


